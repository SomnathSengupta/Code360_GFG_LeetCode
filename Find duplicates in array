from sys import *
from collections import *
from math import *

def findDuplicate(arr:list, n:int):
    a = sorted(arr)
    for i in range(1 , n):
        if(a[i-1]==a[i]):
            return a[i]
    pass
