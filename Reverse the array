from os import *
from sys import *
from collections import *
from math import *

def reverseArray(arr, m):
    start = M + 1
    end = len(arr) - 1
    while start <= end:
        arr[start], arr[end] = arr[end], arr[start]
        start += 1
        end -= 1
    pass
