from os import *
from sys import *
from collections import *
from math import *

def sumOfMaxMin(arr):
    # Write your code here
    max = arr[0]
    min = arr[0]

    for num in arr:
        if num > max:
            max = num
        elif num < min:
            min = num
    return max + min
        
    pass
